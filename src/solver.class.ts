import { MazeNode } from './maze-node.class'
import { Vector } from './vector.class'

export class Solver {
  public solve(nodes: MazeNode[]) {
    let closedNodes: MazeNode[]
    let openNodes: MazeNode[]

    let path: MazeNode[] = []

    return path
  }
}